<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>TUN.R | <%= @artist.name.upcase %></title>
</head>
<body>
  <h1><%= @artist.name + " | " + @artist.nationality %></h1>
  <img src='<%= @artist.photo_url %>' height='50%' width='50%'/>

</body>
</html>
