<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title></title>
</head>
<body>
  <h1>Hello</h1>
  <% @songs.each do |song| %>
  <h2>Delete Song</h2>

  <p class="song">
    <a href="/songs/<%= song.id %>" >
      <%= song.title + " Album: " + song.album %>
      <% song.preview_url %>
    </a>
      <!-- <img style="height: 10em" src= "<%= artist.photo_url %>" > -->
      <!-- <%= artist.name + " Nationality: " + artist.nationality %> -->
    </p>
    <% end %>
    <h2><a href="/songs/new">Add your song here</a></h2>
</form>
  </body>
  </html>
